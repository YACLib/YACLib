#pragma once

#include <cstdlib>

namespace yaclib::detail {

struct WorkerMetrics {
  // TODO(kononovk): Implement metrics
};

struct PoolMetrics : WorkerMetrics {
  // TODO(kononovk): Implement metrics
};

}  // namespace yaclib::detail
